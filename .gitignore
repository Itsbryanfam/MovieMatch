node_modules
.env
.DS_Store
# Jest coverage output — regenerated by `npm test`, never committed (kept
# untracked deliberately since Phase 2; this just silences the noise).
coverage/
# Local Claude Code agent settings/state — machine-specific, never committed
# (was untracked-but-unignored, so a stray `git add -A` would have captured it).
.claude/
