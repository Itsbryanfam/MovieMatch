{
  "planPath": "docs/superpowers/plans/2026-05-19-chain-premiere-recap.md",
  "tasks": [
    {
      "id": 0,
      "subject": "Task 0: Pure choreography engine + DRY relocation",
      "status": "completed",
      "description": "**Goal:** Create the pure zero-import `chain-recap.js` (`buildRecapStoryboard` + the relocated `selectChainEntries`/`scoreChainEntry`); `ui-sharecard.js` imports them back and re-exports them so its public surface is byte-stable.\n\n**Files:** Create public/js/ui/chain-recap.js + client-tests/chain-recap.test.js; Modify public/js/ui/ui-sharecard.js (remove fn defs at L183-231; add import + re-export after L7).\n\n**Verify:** cd C:/mm-phase7-6 && npx jest client-tests/chain-recap.test.js → PASS; then cd C:/mm-phase7-6 && npx jest → all green (additive over 55/437).\n\n**Steps:** TDD — write chain-recap.test.js (schema/order/determinism, cap reuse, finale-parity for all 5 winner branches, elimination best-effort both-ways, zero-identity sentinel, relocation reference-equality + output parity) → verify FAIL (module missing) → create chain-recap.js (verbatim relocated scoreChainEntry/selectChainEntries + BEAT_MS frozen consts + buildFinalePayload pure replica of showGameOverBanner L872-893 + buildRecapStoryboard intro→(bridge?→link)→skipped?→finale) → edit ui-sharecard.js (import {selectChainEntries,scoreChainEntry} from ./chain-recap.js + re-export; DELETE the two duplicated fn defs) → verify GREEN → full npx jest → branch-verify + commit.\n\n```json:metadata\n{\"files\": [\"public/js/ui/chain-recap.js\", \"public/js/ui/ui-sharecard.js\", \"client-tests/chain-recap.test.js\"], \"verifyCommand\": \"cd C:/mm-phase7-6 && npx jest client-tests/chain-recap.test.js && cd C:/mm-phase7-6 && npx jest\", \"acceptanceCriteria\": [\"chain-recap.js zero-import & pure (no DOM/window/timers/Date/RNG)\", \"buildRecapStoryboard returns spec-§3.1 ordered beats; atMs=cumsum(durMs), index=ordinal, durMs=frozen per-type const\", \"curation reuses relocated selectChainEntries (≤7) + single skipped beat iff skipped>0\", \"finale.winnerLine/subLine char-identical to showGameOverBanner for all 5 winner branches\", \"elimination beat only if entry.eliminated===true; absent on realistic fixtures (no fabrication)\", \"zero-identity sentinel green (no stableId/socket id in any beat)\", \"long no-elim chain total atMs+durMs ≤ 13000\", \"ui-sharecard re-exports SAME refs as chain-recap; output parity holds; server/** untouched; full npx jest green\"]}\n```"
    },
    {
      "id": 1,
      "subject": "Task 1: Recap overlay driver + wiring",
      "status": "completed",
      "blockedBy": [0],
      "description": "**Goal:** Create the thin `recap-player.js` driver, the additive `#recap-overlay` skeleton, the append-only recap CSS, and the one-shot Daily-suppressed wiring in `socketClient.js` — with `ui-render.js`/`showGameOverBanner` byte-identical and the §5 surfaces unchanged.\n\n**Files:** Create public/js/ui/recap-player.js + client-tests/recap-player.test.js; Modify public/index.html (additive #recap-overlay after L564), public/css/04-modals.css (append-only after L610), public/css/06-states-anim.css (append-only after L1214), public/js/socketClient.js (add playRecap to ui.js import L14; one block after the Daily block L499).\n\n**Verify:** cd C:/mm-phase7-6 && npx jest client-tests/recap-player.test.js → PASS; then cd C:/mm-phase7-6 && npx jest → all green; then git diff ed6f041 -- public/js/ui/ui-render.js = EMPTY; CSS/index.html/socketClient diffs additive-only; server/ diff EMPTY.\n\n**Steps:** TDD — write recap-player.test.js (reduced-motion instant-settle+onDone+no-timer, animated path schedules+resolves, Skip→settled, Replay→restart, Close→hide, null mount no-op, poster fallback) → verify FAIL (module missing) → create recap-player.js (buildRecapStoryboard sync; beatNode createElement/textContent + attachPosterFallback; cancelRecap leak-safe; prefersReducedMotion→settle(true); else setTimeout-chain w/ is-in rAF) → add #recap-overlay sibling in index.html after L564 → append recap structural CSS to 04-modals.css EOF + compositor entrance to 06-states-anim.css EOF → add playRecap to socketClient ui.js import + the `if (justFinished && !getIsDaily())` block after the Daily block → verify GREEN → diff-gate (ui-render.js EMPTY; server/ EMPTY; CSS append-only; index.html additive sibling; socketClient import+one-block only) → full npx jest → branch-verify + commit.\n\n```json:metadata\n{\"files\": [\"public/js/ui/recap-player.js\", \"public/index.html\", \"public/css/04-modals.css\", \"public/css/06-states-anim.css\", \"public/js/socketClient.js\", \"client-tests/recap-player.test.js\"], \"verifyCommand\": \"cd C:/mm-phase7-6 && npx jest client-tests/recap-player.test.js && cd C:/mm-phase7-6 && npx jest\", \"acceptanceCriteria\": [\"playRecap builds storyboard sync, renders into #recap-overlay, removes hidden\", \"prefersReducedMotion → instant settled end-state + onDone, no timer\", \"animated path = single cancellable setTimeout chain; cancelRecap leak-safe\", \"#recap-skip→settled, #recap-replay→restart, #recap-close→hide; posters via attachPosterFallback\", \"wiring fires once on justFinished && !getIsDaily(); never Daily; never on re-render\", \"ui-render.js byte-identical vs ed6f041; socketClient.js = import add + one block only; CSS append-only no new @media/colour; index.html additive sibling only\", \"render-chain/socket-handlers/showScreen/lobby suites/server/** byte-identical & green; full npx jest green\"]}\n```"
    },
    {
      "id": 2,
      "subject": "Task 2: Focused Share Cards 2.0",
      "status": "completed",
      "blockedBy": [1],
      "description": "**Goal:** Add the spoiler-free emoji grid + \"I survived N links\" line to the existing share card (canvas + text recap), additively — existing `ui-sharecard.js` exports byte-stable, `#share-modal` markup + Share button + modal wiring unchanged.\n\n**Files:** Modify public/js/ui/ui-sharecard.js (add buildEmojiGrid + survivedLine; additive draws in generateShareCard; additive lines in buildTextRecap); Create client-tests/sharecard.test.js.\n\n**Verify:** cd C:/mm-phase7-6 && npx jest client-tests/sharecard.test.js → PASS; then cd C:/mm-phase7-6 && npx jest → all green; ui-sharecard.js diff additive-only; #share-modal markup unchanged; server/ diff EMPTY.\n\n**Steps:** TDD — write sharecard.test.js (buildEmojiGrid one-emoji-per-curated seed🎬/last🏁/spicy🔥/else🟦 + ' +N' iff skipped + no titles/names/identifiers; survivedLine first-person integer-only no identifier; buildTextRecap additive incl all original lines; byte-stable exports; generateShareCard still 600x720 canvas) → verify FAIL (buildEmojiGrid not a function) → add buildEmojiGrid + survivedLine pure exports (reuse selectChainEntries/scoreChainEntry) → additively push grid+survived lines in buildTextRecap before the siteUrl block → additively draw grid+survived on canvas after billing block before footerGrad (reuse COLORS, no new colour) → verify GREEN → diff-gate (ui-sharecard additive-only; #share-modal unchanged; server/ EMPTY) → full npx jest → branch-verify + commit.\n\n```json:metadata\n{\"files\": [\"public/js/ui/ui-sharecard.js\", \"client-tests/sharecard.test.js\"], \"verifyCommand\": \"cd C:/mm-phase7-6 && npx jest client-tests/sharecard.test.js && cd C:/mm-phase7-6 && npx jest\", \"acceptanceCriteria\": [\"buildEmojiGrid: one emoji/curated entry (seed🎬/last🏁/spicy🔥/else🟦) + ' +N' iff skipped; no titles/names/identifiers\", \"survivedLine: '🔗 I survived N links', first-person integer-only, no identifier\", \"buildTextRecap additively includes grid+survived AND all original lines\", \"generateShareCard still returns the 600x720 canvas; additive draws reuse COLORS (no new colour); no existing draw removed\", \"all pre-7.6 exports still present (byte-stable surface); #share-modal markup + Share/Download/Copy wiring unchanged\", \"zero-identity sentinel green; server/** untouched; full npx jest green\"]}\n```"
    }
  ],
  "lastUpdated": "2026-05-19T16:30:00Z"
}
